# ADR-NNNN: <Title>

## Status

Proposed | Accepted | Deprecated | Superseded by ADR-NNNN

## Context

<2-4 sentences describing the forces that motivated this decision: what changed,
what constraint we hit, what we observed in production, what option set the
team is choosing between.>

## Decision

<1-2 sentences stating exactly what we decided. The decision is the
short, declarative answer to the context — not the rationale.>

## Consequences

### Positive

- ...

### Negative / Trade-offs

- ...

### Neutral

- ...

## Alternatives Considered

- **Alt 1:** <name> — rejected because <one-line reason>.
- **Alt 2:** <name> — rejected because <one-line reason>.

## References

- Code: <`src/...` paths if helpful, with line numbers>
- Related ADRs: ADR-NNNN, ADR-MMMM
- External links: <RFCs, blog posts, vendor docs>

---

<!--
Authoring notes (delete before merging):

1. Pick the next free integer from `ls docs/adr/`; zero-pad to 4 digits.
2. Slug is kebab-case, <= 6 words.
3. Keep the ADR under 400 words. Longer ADRs are usually a sign the discussion
   isn't ready.
4. Reference this ADR from CHANGELOG.md under `### Added` or
   `### Changed`, flagged **Breaking**, with a backwards-compatibility note.
   A protected-path change also needs a `BREAKING-CHANGE` commit trailer.
-->
